name: Bug-test
kind: workflow
apiVersion: automation.cloudbees.io/v1alpha1

on:
  push:
    branches:
      - 'main'

jobs:
  job-1:
    steps:
      - name: Publish workflow evidence item
        uses: cloudbees-io/publish-evidence-item@v1
        with:
          content: "name:Akashsami, Age:26"
          format: "MARKDOWN"
          
  job/2:
    steps:
      - name: Publish workflow evidence item
        uses: cloudbees-io/publish-evidence-item@v1
        with:
          content: "name:Akashsami, Age:26, evidence: 2"
          format: "MARKDOWN"
